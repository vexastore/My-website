# Worklog — 17 September 2026

## Local checkout handoff and variant pricing (10:10 EEST)

### Summary

Separated mock from real checkout confirmation and added automatic WhatsApp handoff after real persistence. Staged a database migration for selected-option validation and pricing.

### Why

A local mock confirmation had been mistaken for an admin order; the expected WhatsApp navigation was absent.

### How

The development-only mock marker passes from the order route to checkout. Real orders synchronously save a local receipt and cleared cart before navigating to the encoded store chat. Variant price deltas are used in receipts and the WhatsApp summary.

### When

17 September 2026, 10:10 Asia/Beirut.

### Where

`app/api/orders/route.ts`, `src/components/Checkout.tsx`, `src/context/ShopContext.tsx`, `src/types.ts`, `src/utils/whatsapp.ts`, `src/utils/pricing.ts`, `lib/fetchProducts.ts`, `src/components/ProductPage.tsx`, `src/components/MyOrders.tsx`, `tests/whatsapp.test.mjs`, `tests/pricing.test.mjs`, `tsconfig.json`, `README.md`, `CHANGELOG.md`, `docs/architecture.md`, `docs/features.md`, `docs/API.md`, `docs/Database.md`, `docs/Testing.md`, `docs/02-architecture/flows/whatsapp-checkout.md`, this worklog; admin repository migration and SQL fixture documented there.

### Changes Made

- Real orders open WhatsApp with prefilled customer and size-aware line details after the server acknowledges the order.
- Mock orders are identified in the UI, do not enter history, and do not open WhatsApp. Legacy `VX-LOCALTEST` receipts are removed on hydration.
- Cart keys and stock checks distinguish selected sizes; the admin SQL migration validates and prices options atomically when applied.

### Problems Encountered

No server-only Supabase secret is configured locally; the browser test used an in-memory RPC mock. The SQL migration has not yet been exercised against a local PostgreSQL stack.

### Solutions

Labeled mock responses, kept their carts for subsequent real tests, and inspected the live order table read-only.

### Bugs Found

Checkout offered only a manual WhatsApp link; mock success looked like a persisted order. WhatsApp line totals ignored option price deltas.

### Bug Fixes

Automatic navigation now follows real persistence, and size-adjusted line totals are generated.

### Testing

- Unit Tests: six pricing and WhatsApp tests passed, including size deltas and Arabic text.
- Integration Tests: local mock API had returned 201, idempotent replay, invalid-payload 400, and foreign-origin 403; no live order was written.
- End-to-End Tests: browser mock checkout was exercised before the new mock notice; new notice and real navigation need browser verification.
- Manual Testing: live database read-only query found five imported orders and zero storefront orders. TypeScript check passed; production build generated 154 routes; local home, checkout, products, and articles returned 200. Mock-mode notice and live WhatsApp navigation remain unverified in a browser.
- Coverage: focused affected modules; no numeric coverage report.

### Performance Notes

The real checkout adds one navigation after its existing order request; no extra network write. No benchmark collected.

### Advanced Warnings

The migration is staged but not applied. A server-only secret and a controlled real transaction are needed before an order can appear in admin. The public storefront has not been pushed or deployed. WhatsApp opening does not send the message automatically.

## Local storefront smoke test (10:17 Asia/Beirut)

### Summary

Started the Supabase-backed public storefront locally on `127.0.0.1:3000` and verified its read routes. No production deployment or live order write was made.

### Why

The user requested another local run and checkout test before public release.

### How

Ran the Next development server and exercised HTML, JSON, SEO, product-detail, and checkout routes through HTTP. Attempted the browser-control service twice.

### When

17 September 2026, 10:17 Asia/Beirut.

### Where

This worklog only; local development server and temporary test output outside the repository.

### Changes Made

No application code changed. The development server was left running for manual review.

### Problems Encountered

The computer-use browser service reported `Unable to load browser request-header policy` twice, preventing visual and interaction testing. The local server-only Supabase key remains absent.

### Solutions

Used direct read-only HTTP smoke checks and a controlled checkout request that safely returned 503 without an order write.

### Bugs Found

No new application bug confirmed. Browser interaction remains unverified because the browser service is unavailable.

### Bug Fixes

None in this test-only session.

### Testing

- Unit Tests: 6/6 focused pricing and WhatsApp tests passed.
- Integration Tests: home, checkout, category, blog, product detail, products API, articles API, settings API, robots, and sitemap returned 200; products API returned 109 products.
- End-to-End Tests: browser workflow blocked by browser-control service; no real checkout attempted.
- Manual Testing: local checkout POST returned expected 503 with no server-only key; no live order was created. Coverage is read-route and safe failure-path only.

### Performance Notes

No benchmark was collected; the first local server compilation took several seconds.

### Advanced Warnings

A committed-order WhatsApp redirect and admin appearance remain unverified. The database migration is staged and unapplied. The public storefront remains undeployed.

## Isolated staging checkout preparation (17 September 2026)

### Summary

Prepared guarded local staging startup and a matching synthetic checkout seed. Branch creation and credential configuration remain pending; the user declined the paid branch.

### Why

The user wants local checkout to persist test orders without writing to the live store.

### How

A staging-specific environment file launches Next with branch credentials; the launcher rejects the production Supabase hostname and missing keys. The separate admin repository has a matching local staging command.

### When

17 September 2026, Asia/Beirut.

### Where

`package.json`, `.env.staging.example`, `scripts/dev-staging.mjs`, `README.md`, `CHANGELOG.md`, `docs/architecture.md`, `docs/features.md`, `docs/API.md`, `docs/Database.md`, `docs/Testing.md`, this worklog; matching files and seed in the admin repository.

### Changes Made

Added `npm run dev:staging` on port 3001, required branch credentials, and a hard stop if the branch URL is the live project. Documented the local staging admin and production-data separation.

### Problems Encountered

No existing staging branch or local database was available. Supabase branching costs $0.01344/hour; the user chose to keep the prepared local setup without creating a branch.

### Solutions

Prepared reviewable files and guardrails before branch creation.

### Bugs Found

No new runtime bug confirmed. Existing real-order flow cannot run without a server-only key.

### Bug Fixes

No production checkout change; staging launcher prevents accidental live-project use.

### Testing

- Unit Tests: 6/6 pricing and WhatsApp tests passed.
- Integration Tests: staging launcher rejected missing credentials and the live project URL.
- End-to-End Tests: pending branch creation and staging credentials.
- Manual Testing: local storefront remains available on port 3000; no live order created. Coverage includes startup safety only, not a committed staging order.

### Performance Notes

No runtime path changes outside the staging launcher.

### Advanced Warnings

The branch was not created at the user’s request; price migration, seed, staging admin account, and local secret remain unapplied/unconfigured. Staging orders will not appear in the production admin. No public deployment was made.

## Storefront production cutover preparation (15:18 Asia/Beirut)

### Summary

Prepared the Supabase storefront and WhatsApp checkout for the authorized production release. The size-aware order migration is applied in the separate admin repository; this entry records the storefront code and release checks before deployment.

### Why

The public site still used Firebase-backed catalog and checkout paths, and the installed Next.js version had a critical production advisory. The admin and public stores need one validated order boundary before the owner can test live checkout.

### How

The storefront now reads catalog, article, media, and settings data from Supabase. Checkout sends a validated same-origin request to a server-only `create_order` RPC and opens a prefilled WhatsApp chat only after the order is saved. Removed unused privileged upload, migration, notification, IndexNow, and cache-revalidation routes. Patched production dependencies and restricted remote image patterns to the Supabase project.

### When

17 September 2026, 15:18 Asia/Beirut; production deployment and post-deployment checks follow this preparation entry.

### Where

`.gitignore`, `api/` retired routes, `app/` pages and API routes, `lib/` data access, `src/` storefront and checkout components, `middleware.ts`, `next.config.mjs`, `package.json`, `package-lock.json`, `tsconfig.json`, `scripts/dev-staging.mjs`, `tests/`, `README.md`, `CHANGELOG.md`, `docs/index.md`, `docs/architecture.md`, `docs/features.md`, `docs/API.md`, `docs/Database.md`, `docs/Testing.md`, `docs/02-architecture/`, `docs/04-development/`, `docs/09-project/`, and this worklog.

### Changes Made

- Migrated public catalog, content, settings, product media, and checkout reads/writes to Supabase; linked option pricing and stock to persisted orders.
- Added automatic WhatsApp opening after order persistence, with a receipt link for retrying the handoff; local mock confirmations remain visibly separate.
- Removed legacy Firebase admin and exposed backend routes and shortened catalog revalidation to five minutes.
- Upgraded Next.js to 15.5.24, overrode patched PostCSS and Sharp versions, removed unused Vercel Blob package, and bumped the storefront to 1.1.0.
- Prepared guarded local staging commands without creating a paid staging branch.

### Problems Encountered

The owner had to set the server-only Vercel production secret. The local checkout environment still lacks that secret, so a real local transaction is unavailable. The browser-control service previously failed to load its request-header policy.

### Solutions

Verified the Vercel production environment contains all three required Supabase variable names without printing their values. Applied the order migration in Supabase through the admin release and verified its SQL fixture inside a rollback transaction. Retained safe invalid-order production smoke checks for after deployment.

### Bugs Found

Previous checkout could appear successful without a database order, did not automatically hand off to WhatsApp, and omitted selected-size price deltas. Legacy privileged routes remained public.

### Bug Fixes

Success follows confirmed persistence; WhatsApp uses the persisted order; option pricing uses selected sizes; unused privileged routes were removed.

### Testing

- Unit Tests: 6/6 pricing and WhatsApp tests passed after the dependency update.
- Integration Tests: admin Supabase SQL order fixture passed inside a rollback transaction; storefront production build and TypeScript check passed.
- End-to-End Tests: full storefront browser checkout is pending a controlled live order by the owner. Earlier local read-route smoke checks passed.
- Manual Testing: verified production environment variable names and zero findings from `npm audit --omit=dev --package-lock-only`; no live order was created.
- Coverage: focused checkout, pricing, migration, and read routes; no numeric coverage report.

### Performance Notes

Catalog pages revalidate within five minutes. Image delivery is restricted to WebP. No performance benchmark was collected.

### Advanced Warnings

WhatsApp requires the customer to tap Send; the site cannot send a message from a regular WhatsApp account. Production checkout, admin order appearance, and the WhatsApp app handoff remain to be tested with a real owner order. The local staging branch was not created at the owner's request.

## Storefront 1.1.0 production deployment (15:25 Asia/Beirut)

### Summary

Deployed the Supabase storefront to `vexatoys.com`. Production read routes, SEO routes, and safe checkout failure paths passed. The first real order and WhatsApp handoff remain for the store owner to test.

### Why

The owner authorized both repositories for production testing after adding the checkout server secret to Vercel.

### How

Pushed commit `432895c` and deployed production artifact `dpl_8g3gxnfiMydTGgGjiGNfHVt8TLuk` with Vercel CLI. Checked HTTP responses and queried live order counts after invalid-order tests.

### When

17 September 2026, 15:25 Asia/Beirut.

### Where

`README.md`, `CHANGELOG.md`, `docs/architecture.md`, `docs/features.md`, `docs/Database.md`, `docs/Testing.md`, this worklog; Vercel project `my-website` and domain `vexatoys.com`.

### Changes Made

Recorded the Ready production status and updated documentation from local candidate to live cutover. The live site now uses Supabase catalog, content, settings, and order boundaries.

### Problems Encountered

Admin app 0.9.1 deployment was independently blocked by Vercel team access; admin 0.9.0 remains live. A controlled real owner order was not available during automated checks.

### Solutions

Separated admin app release status from the live database migration and storefront deployment. Used valid-shape requests with nonexistent product IDs to exercise the server-only checkout path without creating orders.

### Bugs Found

Production documentation still described the cutover and order-option migration as pending.

### Bug Fixes

Updated release, architecture, database, feature, and testing pages to reflect the verified live state.

### Testing

- Unit Tests: storefront pricing and WhatsApp 6/6 passed before release.
- Integration Tests: live `/api/products` returned 109 items, `/api/articles` returned 3, `/api/store-settings` returned settings, and a valid-format order for a nonexistent product returned 409.
- End-to-End Tests: full buyer browser checkout remains pending owner test; admin E2E 7/7 passed locally but 0.9.1 app deployment is blocked.
- Manual Testing: Vercel deployment Ready and aliased to `vexatoys.com`; home, checkout, sitemap, robots, and APIs returned 200; retired IndexNow, revalidation, and Telegram routes returned 404; foreign-origin order returned 403, malformed order returned 400, nonexistent product returned 409. Live order count remained five imported and zero storefront orders.
- Coverage: production read, SEO, route removal, and safe order failure paths; no numeric coverage report.

### Performance Notes

Vercel production build completed; no latency benchmark was collected. Catalog cache refreshes in about five minutes.

### Advanced Warnings

A real customer order must be placed by the owner to verify admin appearance, stock decrement, and WhatsApp opening. WhatsApp still requires the customer to tap Send. Admin 0.9.1 is not deployed until team access is approved.

## WhatsApp order template and image links (15:38 Asia/Beirut)

### Summary

Prepared storefront 1.2.0 with owner-requested structured English and Arabic WhatsApp order drafts and public product image links. The separate admin repository adds order-item thumbnails.

### Why

The prior text omitted order date, subtotal, delivery, SKU, and clear sections; the owner wants product imagery alongside order specifics.

### How

Mapped product and option SKUs from Supabase into cart records. After confirmed order creation, read its stored delivery fee and timestamp. The formatter groups customer, product, and totals sections; selects an option SKU before the product SKU; and includes HTTPS image URLs when available.

### When

17 September 2026, 15:38 Asia/Beirut.

### Where

`src/utils/whatsapp.ts`, `src/types.ts`, `src/context/ShopContext.tsx`, `lib/fetchProducts.ts`, `app/api/orders/route.ts`, `tests/whatsapp.test.mjs`, `package.json`, `package-lock.json`, `README.md`, `CHANGELOG.md`, `docs/architecture.md`, `docs/features.md`, `docs/API.md`, `docs/Testing.md`, and this worklog.

### Changes Made

Added the exact English/Arabic message sections, Beirut date, optional SKU and image URL, and authoritative delivery fee. Bumped version to 1.2.0.

### Problems Encountered

The public product model did not include SKU; a regular WhatsApp URL cannot attach an image file.

### Solutions

Included SKU in existing public catalog mapping and a public HTTPS image link in each applicable item block. An absent image or SKU is omitted.

### Bugs Found

The prior message lacked the requested breakdown and could use stale client delivery fee for its totals.

### Bug Fixes

The order endpoint now returns the committed delivery fee and timestamp where available; message formatting uses them.

### Testing

- Unit Tests: six storefront tests passed, including exact English and Arabic drafts, multi-item grouping, missing SKU/image, and option SKU priority.
- Integration Tests: TypeScript check and production build passed with the new Supabase SKU selection.
- End-to-End Tests: no live customer order was created; owner browser and WhatsApp app handoff remain to verify.
- Manual Testing: decoded `wa.me` links were compared against the approved examples without sending messages.
- Coverage: formatter and affected catalog/order build paths; no numeric coverage report.

### Performance Notes

The order endpoint makes one additional keyed read for committed delivery fee and timestamp. Public catalog still caches for five minutes.

### Advanced Warnings

WhatsApp preloads text only; it cannot attach an image file or send automatically. Image links point at current public media. The separate admin image release still depends on Vercel team access approval.

## WhatsApp template production verification (15:42 Asia/Beirut)

### Summary

Storefront 1.2.0 was deployed Ready and aliased to `vexatoys.com`. The new WhatsApp template and product image links are live.

### Why

The owner needs to test the professional order draft in production.

### How

Deployed commit `7f6f710` through Vercel and exercised production read routes plus a valid-shaped request referencing a nonexistent product, which safely failed before any order write.

### When

17 September 2026, 15:42 Asia/Beirut.

### Where

`README.md`, `CHANGELOG.md`, this worklog; Vercel deployment `dpl_7rZL6wKGcaH8GVcJHLzEebtzqhFP`.

### Changes Made

Recorded the Ready deployment and catalog SKU availability.

### Problems Encountered

No current published product has a product-level SKU in the catalog data, although all 109 have a public image URL.

### Solutions

The draft omits unavailable SKU values instead of inventing them. A real SKU can be entered in admin for future messages.

### Bugs Found

No new production bug found during safe smoke checks.

### Bug Fixes

None after deployment.

### Testing

- Unit Tests: six storefront tests passed before deployment.
- Integration Tests: production `/api/products` returned 109 products with 109 image URLs; a valid-shaped nonexistent-product order returned 409 without a write.
- End-to-End Tests: live customer WhatsApp app handoff remains for owner verification; no automated message was sent.
- Manual Testing: `/`, `/checkout`, and `/sitemap.xml` returned 200; Vercel reported Ready and aliased the deployment.
- Coverage: production read and safe order failure paths; no numeric coverage report.

### Performance Notes

No latency benchmark collected.

### Advanced Warnings

Imported product SKUs remain unset. WhatsApp image URLs are text links, not attached files. Admin 0.10.0 is still blocked by Vercel team contributor access.

## Public order status synchronization (15:56 Asia/Beirut)

### Summary

Deployed storefront 1.2.1 to refresh My Orders from the confirmed Supabase status. The first real storefront order is confirmed in the database, while its browser receipt remained pending because that screen previously read local storage only.

### Why

Admin status updates must appear to the customer without exposing private order details or allowing public order mutations.

### How

Added a bounded same-origin POST endpoint that accepts saved reference and checkout-phone pairs, reads with the server-only Supabase key, and returns only statuses for exact matches. The client refreshes on receipt hydration, tab return, and every minute while open; it persists valid updates to the local receipt and shows an error/retry notice if the lookup fails. Added Confirmed to the storefront status model and labels. Guarded initial receipt persistence until hydration completes.

### When

17 September 2026, implementation at 15:56 Asia/Beirut; production deployment at 15:58.

### Where

`app/api/orders/status/route.ts`, `src/utils/order-status.ts`, `src/types.ts`, `src/context/ShopContext.tsx`, `src/components/MyOrders.tsx`, `src/components/Navbar.tsx`, `tests/order-status.test.mjs`, `package.json`, `package-lock.json`, `README.md`, `CHANGELOG.md`, `docs/API.md`, `docs/Database.md`, `docs/architecture.md`, `docs/features.md`, `docs/Testing.md`, and this worklog. Vercel deployment `dpl_Em8cjndz81hETTeAKNjvGho14soY`.

### Changes Made

Added read-only receipt status lookup, confirmed labels, periodic and focus-triggered refresh, stale-status warning, hydration-safe local persistence, and a detail-view button in the navbar order panel. Bumped version to 1.2.1 and deployed to `vexatoys.com`. No database migration or admin change is required.

### Problems Encountered

Existing receipts stored only the public reference, not the order UUID or a dedicated tracking token. The local development environment has no server secret for a successful database lookup.

### Solutions

Use the existing random storefront reference with the exact checkout phone to authorize status-only reads, while retaining support for the user's existing receipt. Test local denial/failure paths and use a synthetic unmatched reference for production smoke.

### Bugs Found

My Orders and the navbar kept the checkout-time pending status; both lacked a Confirmed label. The initial local-storage write could run before receipt hydration.

### Bug Fixes

The confirmed status now replaces pending after a matching server read, and initial storage persistence waits for hydration.

### Testing

- Unit Tests: ten storefront tests passed, including status merge, matching-phone filter, invalid status rejection, pricing, and WhatsApp formatting.
- Integration Tests: local route returned 403 for a foreign origin, 400 for malformed input, and 503 for a valid lookup without the local server secret; TypeScript and production build passed.
- End-to-End Tests: headless Chrome with a saved pending receipt and mocked status response showed Confirmed in both the navbar panel and My Orders detail; a 503 mock preserved Pending and showed the retry warning. The owner still needs to verify their real browser receipt.
- Manual Testing: read-only Supabase query showed one confirmed storefront order matching the expected reference and phone format, without returning customer details. Vercel deployment Ready and aliased to `vexatoys.com`; production home and catalog returned 200. A synthetic unmatched receipt returned 200 with an empty order list and `no-store`; foreign origin returned 403, malformed input 400, and GET 405. No live customer receipt was queried.
- Coverage: status validation, client merge, local endpoint denial/failure, build; no numeric coverage report.

### Performance Notes

At most one bounded status batch per 20 saved receipts runs on load/tab return and once per minute while the tab is open. Requests and responses are no-store. No latency benchmark collected.

### Advanced Warnings

This browser-only receipt history is not portable across devices. Legacy receipts without a matching storefront reference/phone cannot be refreshed. No durable per-IP rate limiter is attached to this read-only endpoint; add one if abuse emerges. Existing order status remains visible if the lookup fails, with a retry warning.
